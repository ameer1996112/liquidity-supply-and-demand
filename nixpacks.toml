# Nixpacks configuration for Railway - Backend Service

# Use the Python provider which handles pip correctly
providers = ["python"]

[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.install]
cmds = ["pip3 install -r backend/requirements.txt"]

[phases.build]
cmds = []

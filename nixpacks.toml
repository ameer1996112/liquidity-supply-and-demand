# Nixpacks configuration for Railway
# Build from the backend subdirectory

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && python trading_bot.py"

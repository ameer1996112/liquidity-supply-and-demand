# Phase 4 Plan: Automated Error-to-Ticket Pipeline

## Goal
Upgrade the Python backend (`worker.py`) to automatically catch critical trading exceptions and generate High Priority Jira Bug tickets immediately on the Atlassian board.

## Proposed Changes

### Jira Adapter
- [NEW] `src/adapters/jira.py`
  - Implement a `JiraAdapter` class with `create_bug_ticket(title, description)`.
  - Use `requests` to securely hit `https://{JIRA_DOMAIN}/rest/api/3/issue` using the `.env` credentials (`JIRA_EMAIL` & `JIRA_API_TOKEN`).
  - Automatically map to `JIRA_PROJECT_KEY`.

### Worker Loop
- [MODIFY] `src/worker.py`
  - Import the new `JiraAdapter`.
  - Inside the main `while True:` loop's final `except Exception as e` catcher (around line 1848), format the full traceback.
  - Call the Jira creation endpoint (ideally fire-and-forget so we don't block the worker loop).

### Environment Documentation
- [MODIFY] `.env.example`
  - Document the required `JIRA_DOMAIN`, `JIRA_EMAIL`, `JIRA_API_TOKEN`, and `JIRA_PROJECT_KEY` variables needed for backend execution logging.

## Verification
- I will temporarily inject a `raise ValueError("Manual AI Ticket Test")` inside `worker.py` and run it locally.
- We will verify the terminal successfully captures the error and that a new Bug Ticket successfully appears on your `ameer1996112.atlassian.net` board.

---
wave: 1
depends_on: "Nothing"
files_modified: "migrations/047_prop_firm_data_foundation.sql, migrations/048_add_challenge_type.sql"
autonomous: true
---

# Plan 01-01: Schema Migrations and Seed Data

## Goal
Create database tables for prop firm server mappings and rules, add the `challenge_type` column to `broker_profiles`, and seed FTMO data.

## Must Haves
- `prop_firm_server_mappings` table
- `prop_firm_rules` table
- `challenge_type` column on `broker_profiles`
- FTMO seed data
- Covers requirement IDs: DATA-01, DATA-02, DATA-03

## Tasks

### 1. Create Data Foundation Migration
<read_first>
- migrations/RELEASE_PROCESS.md
- migrations/046_be_trigger.sql
- .planning/phases/01-data-foundation-and-bug-fixes/01-RESEARCH.md
</read_first>
<action>
Create a new file `migrations/047_prop_firm_data_foundation.sql`.
Add the SQL to create `prop_firm_server_mappings` and `prop_firm_rules` tables, exactly as specified in the Phase 1 RESEARCH.md Code Examples section.
Also add `INSERT INTO public.prop_firm_server_mappings` and `INSERT INTO public.prop_firm_rules` to seed FTMO Phase 1, Phase 2, and Funded rules as specified in RESEARCH.md.
</action>
<acceptance_criteria>
- `migrations/047_prop_firm_data_foundation.sql` contains `CREATE TABLE IF NOT EXISTS public.prop_firm_server_mappings`
- `migrations/047_prop_firm_data_foundation.sql` contains FTMO seed statements
</acceptance_criteria>

### 2. Create Broker Profiles Migration
<read_first>
- migrations/021_per_account_evaluation.sql
- .planning/phases/01-data-foundation-and-bug-fixes/01-RESEARCH.md
</read_first>
<action>
Create a new file `migrations/048_add_challenge_type.sql`.
Add the SQL: `ALTER TABLE public.broker_profiles ADD COLUMN IF NOT EXISTS challenge_type text CHECK (challenge_type IN ('phase_1', 'phase_2', 'funded'));`
This fulfills API requirements to store the challenge type without breaking existing `evaluation_phase` code.
</action>
<acceptance_criteria>
- `migrations/048_add_challenge_type.sql` contains `ALTER TABLE public.broker_profiles ADD COLUMN IF NOT EXISTS challenge_type`
</acceptance_criteria>

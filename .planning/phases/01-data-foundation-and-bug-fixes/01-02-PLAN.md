---
wave: 2
depends_on: "01-01-PLAN.md"
files_modified: "src/services/prop_firm_tracker.py, src/services/mtm_guardian.py, tests/test_prop_firm_phase1.py"
autonomous: true
---

# Plan 01-02: Fix Calculation Bugs

## Goal
Fix six known bugs in the prop firm tracker and MTM guardian regarding timezones, drawdowns, pip calculations and silent exceptions.

## Must Haves
- NY Midnight used for daily drawdown resets
- Equity used for daily start baseline
- Funded drawdown uses HWM, Phase 1/2 use starting balance
- trades_today queries correctly
- save_snapshot propagates errors
- JPY pip value dynamically calculated in MTM
- Covers requirement IDs: BUG-01, BUG-02, BUG-03, BUG-04, BUG-05, BUG-06

## Tasks

### 1. Fix NY Midnight Boundary and Daily Baseline (BUG-01, BUG-02)
<read_first>
- src/services/prop_firm_tracker.py
- .planning/phases/01-data-foundation-and-bug-fixes/01-RESEARCH.md
</read_first>
<action>
In `src/services/prop_firm_tracker.py`, import `pytz`. Add a module-level helper `get_ny_midnight_utc() -> datetime` that returns today's NY midnight as UTC (as shown in RESEARCH.md).
Replace all instances of `datetime.now(timezone.utc).replace(hour=0...` with a call to `get_ny_midnight_utc()`.
Ensure the daily reset uses equity at day start, not balance.
</action>
<acceptance_criteria>
- `src/services/prop_firm_tracker.py` contains `def get_ny_midnight_utc`
- `src/services/prop_firm_tracker.py` uses `pytz.timezone("America/New_York")`
</acceptance_criteria>

### 2. Fix Drawdown Denominator (BUG-03)
<read_first>
- src/services/prop_firm_tracker.py
</read_first>
<action>
In `src/services/prop_firm_tracker.py` `get_current_metrics()`, add branching for `total_dd_pct` calculation based on the phase.
If `evaluation_phase == "funded"`, use `max_historical_equity` as the denominator.
Otherwise (Phase 1/2), use `starting_balance` from the `broker_profile` record as the denominator.
</action>
<acceptance_criteria>
- `src/services/prop_firm_tracker.py` contains branching for `evaluation_phase == "funded"` denominator calculation
</acceptance_criteria>

### 3. Fix trades_today and silent exceptions (BUG-04, BUG-05)
<read_first>
- src/services/prop_firm_tracker.py
</read_first>
<action>
In `save_snapshot()`, query `trading_signals` for actual trades closed since `get_ny_midnight_utc()` instead of hardcoding `0`.
Remove the `except Exception` block that silently swallows the error, or at least `raise` the error after logging so the caller knows it failed.
</action>
<acceptance_criteria>
- `src/services/prop_firm_tracker.py` queries `self.supabase.table("trading_signals")` for trades
- `src/services/prop_firm_tracker.py` `save_snapshot` uses `raise` in exception blocks or removes the bare except 
</acceptance_criteria>

### 4. Fix JPY dynamic pip value (BUG-06)
<read_first>
- src/services/mtm_guardian.py
- src/core/risk_engine.py
</read_first>
<action>
Simply replicate the `(0.01 / entry_price) * 100000` logic inside `src/services/mtm_guardian.py` replacing the hardcoded `1000.0` value for JPY, exactly as specified in RESEARCH.md Code Examples.
Also use `get_ny_midnight_utc()` in `mtm_guardian.py` if needed.
</action>
<acceptance_criteria>
- `src/services/mtm_guardian.py` contains `(pip_size / entry) * 100000` or equivalent math instead of hardcoded 1000.0 for JPY
</acceptance_criteria>

### 5. Write Tests for Phase 1 Bugs
<read_first>
- tests/conftest.py
</read_first>
<action>
Create `tests/test_prop_firm_phase1.py`.
Write pytest cases for the NY midnight UTC conversion, the `trades_today` calculation, the HWM denominator logic, and the dynamic JPY pip value.
Use the `conftest.py` mocking patterns for Supabase.
</action>
<acceptance_criteria>
- `tests/test_prop_firm_phase1.py` exists
- `cat tests/test_prop_firm_phase1.py` contains `def test_ny_midnight`
</acceptance_criteria>

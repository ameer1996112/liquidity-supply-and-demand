---
phase: 02-core-component-library
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/app/positions/page.tsx
  - frontend/src/app/analytics/page.tsx
  - frontend/src/app/risk/page.tsx
  - frontend/src/app/prop-firm/page.tsx
autonomous: true
requirements: [COMP-06]

must_haves:
  truths:
    - "Dashboard page shows shimmer skeletons while data is loading"
    - "Positions page shows table row skeletons while data is loading"
    - "Analytics page shows chart and stat card skeletons while loading"
    - "Risk page shows metric gauge skeletons while loading"
    - "Prop Firm page shows progress bar and stat card skeletons while loading"
    - "All skeleton wrappers have aria-label for accessibility"
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Dashboard skeleton loading states"
      contains: "Skeleton"
    - path: "frontend/src/app/positions/page.tsx"
      provides: "Positions skeleton loading states"
      contains: "Skeleton"
    - path: "frontend/src/app/analytics/page.tsx"
      provides: "Analytics skeleton loading states"
      contains: "Skeleton"
    - path: "frontend/src/app/risk/page.tsx"
      provides: "Risk skeleton loading states"
      contains: "Skeleton"
    - path: "frontend/src/app/prop-firm/page.tsx"
      provides: "Prop Firm skeleton loading states"
      contains: "Skeleton"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/ui/skeleton.tsx"
      via: "import { Skeleton } from '@/components/ui/skeleton'"
      pattern: "import.*Skeleton.*from.*ui/skeleton"
    - from: "frontend/src/app/positions/page.tsx"
      to: "frontend/src/components/ui/skeleton.tsx"
      via: "import { Skeleton } from '@/components/ui/skeleton'"
      pattern: "import.*Skeleton.*from.*ui/skeleton"
---

<objective>
Add per-section skeleton loading states to the 5 highest-traffic data pages: Dashboard, Positions, Analytics, Risk, and Prop Firm.

Purpose: COMP-06 requires skeleton/shimmer patterns on at least 3 pages. The locked decision specifies exactly these 5 target pages with layout-matching skeleton shapes. This plan depends on Plan 01 (which updates skeleton.tsx to use shimmer animation) being complete first.

Output: 5 page files updated with skeleton loading wrappers that match each page's rendered layout dimensions.
</objective>

<execution_context>
@/Users/ameeramer/dev/projects/galilsoftware/sources/trading/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ameeramer/dev/projects/galilsoftware/sources/trading/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-component-library/02-CONTEXT.md
@.planning/phases/02-core-component-library/02-UI-SPEC.md
@.planning/phases/02-core-component-library/02-RESEARCH.md

<interfaces>
<!-- Skeleton component (after Plan 01 update) -->
import { Skeleton } from "@/components/ui/skeleton"
<!-- Usage: <Skeleton className="h-24 w-full" /> -->
<!-- The component emits: <div data-slot="skeleton" class="skeleton-shimmer rounded-md {className}" /> -->

<!-- Skeleton shape guide from UI-SPEC: -->
<!-- Dashboard: Card skeleton (h-24 w-full), metric value (h-8 w-32), chart (h-48 w-full) -->
<!-- Positions: Table row skeleton (h-10 w-full, repeat 5x) -->
<!-- Analytics: Chart skeleton (h-64 w-full), stat card skeleton (h-16 w-full, repeat 4x) -->
<!-- Risk: Metric gauge skeleton (h-32 w-32 rounded-full), table row (h-10) -->
<!-- PropFirm: Progress bar skeleton (h-4 w-full), stat card skeleton (h-20 w-full) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading states to Dashboard and Positions pages</name>
  <files>frontend/src/app/page.tsx, frontend/src/app/positions/page.tsx</files>
  <read_first>
    - frontend/src/app/page.tsx (current state — identify existing loading/isLoading checks and data sections to wrap)
    - frontend/src/app/positions/page.tsx (current state — identify loading state and table sections)
    - frontend/src/components/ui/skeleton.tsx (confirm the updated skeleton-shimmer class is in place)
    - .planning/phases/02-core-component-library/02-UI-SPEC.md (Skeleton shape guide section — exact dimensions per page)
  </read_first>
  <action>
For each page, find the existing loading state variable (typically `isLoading` from a React Query hook or similar), add a Skeleton import if not already present, and wrap data-dependent sections in conditional rendering.

**Dashboard (page.tsx):**
- Add `import { Skeleton } from "@/components/ui/skeleton"` if not already imported (page.tsx is in the 24 Skeleton importers list, so it likely already has it — check first)
- Find the main data sections (metric cards, signal feed, charts)
- Wrap each section with: `{isLoading ? <SkeletonBlock /> : <ActualContent />}`
- Skeleton shapes for Dashboard:
  - KPI/metric cards area: `<div aria-label="Loading dashboard metrics" className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4"><Skeleton className="h-24 w-full" /><Skeleton className="h-24 w-full" /><Skeleton className="h-24 w-full" /><Skeleton className="h-24 w-full" /></div>`
  - Signal feed / recent trades: `<Skeleton className="h-48 w-full" />`
  - Chart area: `<Skeleton className="h-48 w-full" />`

If the Dashboard already has skeleton loading (since it's already in the Skeleton importers list), inspect the existing skeletons and ensure they use layout-matching dimensions per UI-SPEC. Do NOT duplicate or add redundant skeletons.

**Positions (positions/page.tsx):**
- Add `import { Skeleton } from "@/components/ui/skeleton"` (positions is NOT in the current Skeleton importers list — it needs to be added)
- Find the loading state variable (likely `isLoading` from a data hook)
- Wrap the positions table/list section with skeleton:
  - Table row skeletons: `<div aria-label="Loading positions" className="space-y-2">{Array.from({ length: 5 }).map((_, i) => <Skeleton key={i} className="h-10 w-full" />)}</div>`

If positions page has a different loading mechanism (e.g., SWR, manual fetch), adapt to whatever existing pattern the page uses. Do not introduce a new data fetching pattern.

Important: Skeleton wrappers must have `aria-label="Loading [section name]"` on the container div for accessibility per UI-SPEC copywriting contract.
  </action>
  <verify>
    <automated>cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading && grep -c "Skeleton" frontend/src/app/page.tsx && grep -c "Skeleton" frontend/src/app/positions/page.tsx && grep -c "aria-label" frontend/src/app/positions/page.tsx && cd frontend && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Skeleton" frontend/src/app/page.tsx` returns at least 1 match
    - `grep "Skeleton" frontend/src/app/positions/page.tsx` returns at least 1 match
    - `grep "import.*Skeleton.*from.*ui/skeleton" frontend/src/app/positions/page.tsx` returns 1 match (new import added)
    - `grep "aria-label" frontend/src/app/positions/page.tsx` returns at least 1 match (Loading label)
    - `grep "h-10 w-full" frontend/src/app/positions/page.tsx` returns at least 1 match (table row skeleton shape)
    - `cd frontend && npx next build` exits 0
  </acceptance_criteria>
  <done>Dashboard has skeleton loading for metric cards, signal feed, and chart sections. Positions has skeleton loading for table rows with 5 placeholder rows. Both pages build cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton loading states to Analytics, Risk, and Prop Firm pages</name>
  <files>frontend/src/app/analytics/page.tsx, frontend/src/app/risk/page.tsx, frontend/src/app/prop-firm/page.tsx</files>
  <read_first>
    - frontend/src/app/analytics/page.tsx (current state — identify loading state and chart/stat sections)
    - frontend/src/app/risk/page.tsx (current state — identify loading state and metric/gauge sections)
    - frontend/src/app/prop-firm/page.tsx (current state — identify loading state and progress/stat sections)
    - .planning/phases/02-core-component-library/02-UI-SPEC.md (Skeleton shape guide section — exact dimensions)
  </read_first>
  <action>
For each page, find the existing loading state, ensure Skeleton is imported, and wrap data sections in conditional rendering.

**Analytics (analytics/page.tsx):**
- Confirm `import { Skeleton } from "@/components/ui/skeleton"` exists (analytics IS in the importers list)
- Find chart and stat card data sections
- Skeleton shapes for Analytics:
  - Chart area: `<Skeleton className="h-64 w-full" />`
  - Stat cards grid: `<div aria-label="Loading analytics" className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">{Array.from({ length: 4 }).map((_, i) => <Skeleton key={i} className="h-16 w-full" />)}</div>`

If Analytics already has skeleton wrappers, verify their dimensions match UI-SPEC (h-64 for chart, h-16 for stat cards). Adjust dimensions if they differ.

**Risk (risk/page.tsx):**
- Confirm `import { Skeleton } from "@/components/ui/skeleton"` exists (risk IS in the importers list)
- Find metric/gauge and table sections
- Skeleton shapes for Risk:
  - Metric gauges: `<div aria-label="Loading risk metrics" className="flex gap-4">{Array.from({ length: 3 }).map((_, i) => <Skeleton key={i} className="h-32 w-32 rounded-full" />)}</div>`
  - Table rows below: `<div className="space-y-2">{Array.from({ length: 3 }).map((_, i) => <Skeleton key={i} className="h-10 w-full" />)}</div>`

**Prop Firm (prop-firm/page.tsx):**
- Add `import { Skeleton } from "@/components/ui/skeleton"` if not already present (prop-firm is NOT in the current importers list)
- Find progress bar and stat card sections
- Skeleton shapes for Prop Firm:
  - Progress bars: `<div aria-label="Loading prop firm data" className="space-y-4">{Array.from({ length: 3 }).map((_, i) => <Skeleton key={i} className="h-4 w-full" />)}</div>`
  - Stat cards: `<div className="grid grid-cols-1 md:grid-cols-2 gap-4">{Array.from({ length: 4 }).map((_, i) => <Skeleton key={i} className="h-20 w-full" />)}</div>`

Important: Each skeleton wrapper container must have `aria-label="Loading [section name]"` for accessibility per UI-SPEC copywriting contract.

Important: Do NOT add redundant skeletons if the page already has them. Inspect first, then add or adjust.

Important: All skeleton shapes are per-section, not page-level spinners as per the locked decision "No global page spinner — per-section skeletons only."
  </action>
  <verify>
    <automated>cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading && grep -c "Skeleton" frontend/src/app/analytics/page.tsx && grep -c "Skeleton" frontend/src/app/risk/page.tsx && grep -c "Skeleton" frontend/src/app/prop-firm/page.tsx && cd frontend && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Skeleton" frontend/src/app/analytics/page.tsx` returns at least 2 matches (chart + stat cards)
    - `grep "Skeleton" frontend/src/app/risk/page.tsx` returns at least 2 matches (gauges + table rows)
    - `grep "Skeleton" frontend/src/app/prop-firm/page.tsx` returns at least 2 matches (progress bars + stat cards)
    - `grep "aria-label" frontend/src/app/analytics/page.tsx` returns at least 1 match
    - `grep "aria-label" frontend/src/app/risk/page.tsx` returns at least 1 match
    - `grep "aria-label" frontend/src/app/prop-firm/page.tsx` returns at least 1 match
    - `grep "h-64" frontend/src/app/analytics/page.tsx` returns at least 1 match (chart skeleton height)
    - `grep "rounded-full" frontend/src/app/risk/page.tsx` returns at least 1 match (gauge skeleton shape)
    - `grep "h-4 w-full" frontend/src/app/prop-firm/page.tsx` returns at least 1 match (progress bar skeleton)
    - `cd frontend && npx next build` exits 0
  </acceptance_criteria>
  <done>Analytics has shimmer skeletons for chart (h-64) and stat cards (h-16). Risk has circular gauge skeletons (h-32 w-32 rounded-full) and table row skeletons. Prop Firm has progress bar skeletons (h-4) and stat card skeletons (h-20). All pages have aria-label accessibility labels. Build passes. COMP-06 is satisfied with 5 pages covered.</done>
</task>

</tasks>

<verification>
1. `cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading/frontend && npx next build` — exits 0
2. All 5 target pages have Skeleton imports: `grep -l "import.*Skeleton" frontend/src/app/page.tsx frontend/src/app/positions/page.tsx frontend/src/app/analytics/page.tsx frontend/src/app/risk/page.tsx frontend/src/app/prop-firm/page.tsx` — lists all 5 files
3. All 5 target pages have aria-label on skeleton containers: `grep -l "aria-label.*Loading" frontend/src/app/page.tsx frontend/src/app/positions/page.tsx frontend/src/app/analytics/page.tsx frontend/src/app/risk/page.tsx frontend/src/app/prop-firm/page.tsx` — lists all 5 files
</verification>

<success_criteria>
- All 5 target pages (Dashboard, Positions, Analytics, Risk, Prop Firm) have per-section skeleton loading states
- Skeleton shapes match rendered layout dimensions per UI-SPEC shape guide
- Every skeleton wrapper has aria-label for accessibility
- No global page spinners — per-section skeletons only
- `npx next build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-component-library/02-03-SUMMARY.md`
</output>

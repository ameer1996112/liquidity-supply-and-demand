---
phase: 02-core-component-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/skeleton.tsx
autonomous: true
requirements: [COMP-01, COMP-04, COMP-06]

must_haves:
  truths:
    - "Primary button renders gold background with amber glow on hover"
    - "Destructive button uses --to-error token, not shadcn destructive"
    - "Ghost button hover shows --to-surface-raised background"
    - "Badge default variant uses --to-surface-raised background"
    - "Badge text is font-semibold (600), not font-medium (500)"
    - "Skeleton shows shimmer gradient animation, not solid pulse"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Token-based button variants"
      contains: "glow-amber"
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "Token-based badge variants"
      contains: "to-surface-raised"
    - path: "frontend/src/components/ui/skeleton.tsx"
      provides: "Shimmer skeleton primitive"
      contains: "skeleton-shimmer"
  key_links:
    - from: "frontend/src/components/ui/button.tsx"
      to: "frontend/src/app/globals.css"
      via: "var(--glow-amber) box-shadow token"
      pattern: "glow-amber"
    - from: "frontend/src/components/ui/skeleton.tsx"
      to: "frontend/src/app/globals.css"
      via: ".skeleton-shimmer CSS class (line 966)"
      pattern: "skeleton-shimmer"
---

<objective>
Restyle button, badge, and skeleton primitives to use --to-* design system tokens exclusively, replacing all shadcn default color/ring classes.

Purpose: These three components use cva or simple className strings that reference shadcn theme tokens (bg-primary, bg-accent, etc.). Replacing with --to-* tokens makes them visually consistent with the dark trading terminal aesthetic and Phase 1 design system.

Output: 3 updated component files with zero shadcn default color references remaining.
</objective>

<execution_context>
@/Users/ameeramer/dev/projects/galilsoftware/sources/trading/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ameeramer/dev/projects/galilsoftware/sources/trading/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-component-library/02-CONTEXT.md
@.planning/phases/02-core-component-library/02-UI-SPEC.md
@.planning/phases/02-core-component-library/02-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Token references available in globals.css :root -->
--to-accent-amber: #f0b90b
--to-error / --to-accent-red: #f6465d
--to-surface-raised: #161b22
--to-surface-overlay: #1c2230
--to-surface: #0d1117
--to-text-primary: #e6eaf0
--to-text-secondary: #8b95a5
--to-text-dim: #4d5666
--to-border: #21262d
--to-border-subtle: #161b22
--glow-amber: 0 0 15px rgba(240,185,11,0.4)
--glow-red: 0 0 15px rgba(246,70,93,0.4)

<!-- .skeleton-shimmer class at globals.css:966-974 -->
.skeleton-shimmer {
  background: linear-gradient(90deg, var(--to-surface-raised) 25%, rgba(255,255,255,0.04) 50%, var(--to-surface-raised) 75%);
  background-size: 800px 100%;
  animation: skeleton-shimmer 1.6s ease-in-out infinite;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle button.tsx variant classes to --to-* tokens (COMP-01)</name>
  <files>frontend/src/components/ui/button.tsx</files>
  <read_first>
    - frontend/src/components/ui/button.tsx (current state — see exact cva base and 6 variant strings)
    - .planning/phases/02-core-component-library/02-UI-SPEC.md (Button contract section — exact class replacements)
    - .planning/phases/02-core-component-library/02-RESEARCH.md (code example at line 249-276 — complete replacement strings)
  </read_first>
  <action>
Replace the buttonVariants cva() call. Keep all imports, the Button function, and exports unchanged.

**Base class string** — replace the entire first argument of cva():
```
"inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-semibold transition-all disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none focus-visible:outline-none"
```

Changes from current base:
- `font-medium` becomes `font-semibold`
- REMOVE these classes entirely: `focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive`
- ADD: `focus-visible:outline-none` (after the existing `outline-none`)

**Variant strings** — replace each variant value exactly:
- `default`: `"bg-[var(--to-accent-amber)] text-[#080b10] hover:shadow-[var(--glow-amber)] focus-visible:shadow-[var(--glow-amber)]/50"`
- `secondary`: `"bg-[var(--to-surface-raised)] text-[var(--to-text-primary)] border border-[var(--to-border)] hover:bg-[var(--to-surface-overlay)] focus-visible:shadow-[var(--glow-amber)]/50"`
- `ghost`: `"text-[var(--to-text-secondary)] hover:bg-[var(--to-surface-raised)] focus-visible:shadow-[var(--glow-amber)]/50"`
- `outline`: `"border border-[var(--to-border)] text-[var(--to-text-primary)] hover:bg-[var(--to-surface-raised)] focus-visible:shadow-[var(--glow-amber)]/50"`
- `destructive`: `"bg-[var(--to-error)] text-white hover:bg-[var(--to-error)]/90 focus-visible:shadow-[var(--glow-red)]/50"`
- `link`: `"text-[var(--to-accent-amber)] underline-offset-4 hover:underline"`

**Size variants** — keep ALL size variant strings exactly as they are (h-9, h-8, h-10, size-9, etc.). Do not modify any size class.

**defaultVariants** — keep exactly as-is: `{ variant: "default", size: "default" }`.

**Button function and exports** — no changes. Keep the Slot/asChild pattern, data-slot, data-variant, data-size attributes, and the `export { Button, buttonVariants }` line.
  </action>
  <verify>
    <automated>cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading && grep -c "font-semibold" frontend/src/components/ui/button.tsx && grep -cL "bg-primary\|ring-ring\|font-medium" frontend/src/components/ui/button.tsx && cd frontend && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "font-semibold" frontend/src/components/ui/button.tsx` returns at least 1 match
    - `grep "font-medium" frontend/src/components/ui/button.tsx` returns 0 matches
    - `grep "glow-amber" frontend/src/components/ui/button.tsx` returns at least 1 match
    - `grep "bg-primary" frontend/src/components/ui/button.tsx` returns 0 matches
    - `grep "to-accent-amber" frontend/src/components/ui/button.tsx` returns at least 1 match
    - `grep "to-error" frontend/src/components/ui/button.tsx` returns at least 1 match
    - `grep "ring-ring" frontend/src/components/ui/button.tsx` returns 0 matches
    - `grep "to-surface-raised" frontend/src/components/ui/button.tsx` returns at least 1 match (ghost/secondary hover)
    - `grep "to-surface-overlay" frontend/src/components/ui/button.tsx` returns at least 1 match (secondary hover)
    - `grep "text-\\[#080b10\\]" frontend/src/components/ui/button.tsx` returns at least 1 match (primary button dark text)
    - All 8 size variants unchanged (h-9, h-8, h-10, size-9, size-6, size-8, size-10 still present)
    - `cd frontend && npx next build` exits 0
  </acceptance_criteria>
  <done>All 6 button variants render with --to-* token colors. Primary has gold background with amber glow hover. Destructive uses --to-error. Ghost/secondary/outline use surface tokens. Link uses amber text. No shadcn default color classes remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle badge.tsx variant classes to --to-* tokens (COMP-04)</name>
  <files>frontend/src/components/ui/badge.tsx</files>
  <read_first>
    - frontend/src/components/ui/badge.tsx (current state — see cva base with font-medium and py-0.5)
    - .planning/phases/02-core-component-library/02-UI-SPEC.md (Badge contract section — variant table)
    - .planning/phases/02-core-component-library/02-RESEARCH.md (code example at line 278-301 — complete replacement strings)
  </read_first>
  <action>
Replace the badgeVariants cva() call. Keep all imports, the Badge function, and exports unchanged.

**Base class string** — replace the entire first argument of cva():
```
"inline-flex items-center justify-center rounded-full border border-transparent px-2 py-1 text-xs font-semibold w-fit whitespace-nowrap shrink-0 [&>svg]:size-3 gap-1 [&>svg]:pointer-events-none transition-[color,box-shadow] overflow-hidden"
```

Changes from current base:
- `py-0.5` becomes `py-1` (4px vertical padding per UI-SPEC spacing scale)
- `font-medium` becomes `font-semibold` (weight 600 per UI-SPEC typography contract)
- REMOVE these classes entirely: `focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive`

**Variant strings** — replace each variant value exactly:
- `default`: `"bg-[var(--to-surface-raised)] text-[var(--to-text-primary)]"`
- `secondary`: `"bg-[var(--to-surface)] text-[var(--to-text-secondary)] border border-[var(--to-border)]"`
- `outline`: `"text-[var(--to-text-secondary)] border border-[var(--to-border)]"`
- `destructive`: `"bg-[var(--to-error)]/15 text-[var(--to-error)] border border-[var(--to-error)]/30"`
- `ghost`: `"text-[var(--to-text-dim)]"`

Note: Remove the existing `link` variant entirely. The UI-SPEC badge contract does not include a link variant for badges. If any caller uses `variant="link"` on Badge, it will fall back to default — which is correct.

**defaultVariants** — keep as-is: `{ variant: "default" }`.

**Badge function and exports** — no changes. Keep Slot/asChild pattern, data-slot, data-variant attributes, and `export { Badge, badgeVariants }`.
  </action>
  <verify>
    <automated>cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading && grep -c "font-semibold" frontend/src/components/ui/badge.tsx && grep -c "py-1 " frontend/src/components/ui/badge.tsx && cd frontend && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "font-semibold" frontend/src/components/ui/badge.tsx` returns at least 1 match
    - `grep "font-medium" frontend/src/components/ui/badge.tsx` returns 0 matches
    - `grep "py-1" frontend/src/components/ui/badge.tsx` returns at least 1 match
    - `grep "py-0.5" frontend/src/components/ui/badge.tsx` returns 0 matches
    - `grep "to-surface-raised" frontend/src/components/ui/badge.tsx` returns at least 1 match
    - `grep "to-text-primary" frontend/src/components/ui/badge.tsx` returns at least 1 match
    - `grep "to-text-secondary" frontend/src/components/ui/badge.tsx` returns at least 1 match
    - `grep "to-text-dim" frontend/src/components/ui/badge.tsx` returns at least 1 match (ghost variant)
    - `grep "to-error" frontend/src/components/ui/badge.tsx` returns at least 1 match (destructive variant)
    - `grep "bg-primary" frontend/src/components/ui/badge.tsx` returns 0 matches
    - `grep "bg-secondary" frontend/src/components/ui/badge.tsx` returns 0 matches
    - `grep "ring-ring" frontend/src/components/ui/badge.tsx` returns 0 matches
    - `cd frontend && npx next build` exits 0
  </acceptance_criteria>
  <done>All 5 badge variants render with --to-* token colors. Default uses surface-raised. Destructive uses --to-error with 15% opacity bg. Ghost uses --to-text-dim. Base text is font-semibold (600) with py-1 padding. No shadcn default color classes remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update skeleton.tsx from pulse to shimmer class (COMP-06 primitive)</name>
  <files>frontend/src/components/ui/skeleton.tsx</files>
  <read_first>
    - frontend/src/components/ui/skeleton.tsx (current state — one-line className with bg-accent animate-pulse)
    - frontend/src/app/globals.css (search for .skeleton-shimmer near line 966 to confirm class exists)
  </read_first>
  <action>
Replace the className string in the Skeleton component's div element.

**Current className:**
```
cn("bg-accent animate-pulse rounded-md", className)
```

**New className:**
```
cn("skeleton-shimmer rounded-md", className)
```

This is a one-line change. The `skeleton-shimmer` class is already defined in globals.css (line 966-974) with the correct gradient shimmer animation (left-to-right, 1.6s ease-in-out infinite). No CSS additions needed.

Do NOT change the component function signature, data-slot attribute, or export statement.

After updating, verify that no Skeleton callers across the codebase pass `animate-pulse` or `bg-accent` as className overrides (which would fight the new shimmer). Run: `grep -r "Skeleton.*animate-pulse\|Skeleton.*bg-accent" frontend/src/`. The expected result is 0 matches.
  </action>
  <verify>
    <automated>cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading && grep -c "skeleton-shimmer" frontend/src/components/ui/skeleton.tsx && grep -rL "Skeleton.*animate-pulse" frontend/src/ | head -1 && cd frontend && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "skeleton-shimmer" frontend/src/components/ui/skeleton.tsx` returns exactly 1 match
    - `grep "animate-pulse" frontend/src/components/ui/skeleton.tsx` returns 0 matches
    - `grep "bg-accent" frontend/src/components/ui/skeleton.tsx` returns 0 matches
    - `grep -r "Skeleton.*className.*animate-pulse" frontend/src/` returns 0 matches (no callers override with pulse)
    - `grep -r "Skeleton.*className.*bg-accent" frontend/src/` returns 0 matches (no callers override with bg-accent)
    - `cd frontend && npx next build` exits 0
  </acceptance_criteria>
  <done>Skeleton component renders shimmer gradient animation instead of solid pulse. The .skeleton-shimmer class from globals.css provides left-to-right gradient sweep at 1.6s. All 24 Skeleton import sites automatically get the new shimmer animation. No caller-side overrides conflict with the new class.</done>
</task>

</tasks>

<verification>
Run all three checks in sequence:
1. `cd /Users/ameeramer/dev/projects/galilsoftware/sources/trading/frontend && npx next build` — exits 0 (TypeScript + Tailwind compilation clean)
2. `grep -rn "bg-primary\|bg-secondary\|bg-destructive\|ring-ring\|bg-accent animate-pulse\|text-muted-foreground\|hover:bg-accent " frontend/src/components/ui/button.tsx frontend/src/components/ui/badge.tsx frontend/src/components/ui/skeleton.tsx` — returns 0 matches (no shadcn defaults remain in any of the 3 files)
3. `grep -cn "glow-amber\|to-surface-raised\|to-text-primary\|skeleton-shimmer" frontend/src/components/ui/button.tsx frontend/src/components/ui/badge.tsx frontend/src/components/ui/skeleton.tsx` — returns matches in all 3 files
</verification>

<success_criteria>
- button.tsx: 6 variant strings use --to-* tokens, gold glow hover on primary, amber focus ring on all
- badge.tsx: 5 variant strings use --to-* tokens, font-semibold base, py-1 spacing
- skeleton.tsx: skeleton-shimmer class replaces bg-accent animate-pulse
- Zero shadcn default color classes (bg-primary, bg-secondary, bg-destructive, bg-accent, text-muted-foreground, ring-ring) remain in any of the 3 files
- `npx next build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-component-library/02-01-SUMMARY.md`
</output>

# Phase 1: Codebase Cleanup & DDD Unification

## Objective
Remove all unused files/folders, consolidate helpers, and ensure clean structural boundaries reflecting DDD across the main directories (`app`/`src`, `frontend`, `worker`).

## Current State
The project has accumulated technical debt via dead files, deprecated tracking, and duplicated functionality. Multiple `__pycache__` directories, legacy notebooks, or test folders might be lingering.

## Execution Steps
1. **Directory Consolidation and Pruning**: 
   - Identify unused and redundant files across the repository via `list_dir`. Let's specifically look for duplicate versions of the API/worker (e.g. `src/` vs `app/`) to eliminate redundancy.
   - Clean out `.__pycache__` artifacts and any standalone legacy scripts.
2. **Helper Deduplication**:
   - Merge duplicate logging, settings, or database utility scripts.
3. **Commit Phase 1 Output**:
   - Save progress to Git if significant deletions are made.

## Must-Haves
- `[ ]` No unused files exist in the repository
- `[ ]` DDD layer structure is strictly enforced (clean split between Frontend, Backend API, Worker, and shared Lib).

## Hand-off Notes
Begin by analyzing the repository structure dynamically using `find_by_name` and `list_dir` to pinpoint the dead code.

# Phase 4: Phase Auto-Detection - Plan 02 (Frontend)

## Goal
Clean up the React frontend components from the legacy Phase Selection manual workflow.

## Implementation Steps
### 1. Wipe `ChallengeSelector.tsx`
Delete `frontend/src/components/prop-firm/ChallengeSelector.tsx`.

### 2. Streamline `PropFirmSection.tsx`
Remove the conditional block displaying `<ChallengeSelector />`.
If `firm_info` exists, just render `WarningBanner` and `ProgressBars` safely using `|| 0` defaults for unpopulated metrics.

## Verification Method
Spin up the `npm run dev` and ensure frontend compiles cleanly with no missing UI components and no hanging conditional trees.

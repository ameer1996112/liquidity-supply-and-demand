# Phase 4: Phase Auto-Detection - Plan 01 (Backend)

## Goal
Implement keyword-based rule parsing to automatically classify accounts into `phase_1`, `phase_2`, or `funded`.

## Implementation Steps
### 1. `PropFirmDetector` Update
Modify `src/services/prop_firm_detector.py` to include:
```python
def auto_detect_challenge_type(self, server_name: str, account_name: str) -> str:
    # Add rules prioritizing server strings -> account name string parsing.
```

### 2. `api_prop_firm_v1.py` Endpoint
Inject the dynamic detection methodology into `/challenge-status/{account_name}` before fetching.
Remove the `/challenge-config/{account_name}` PATCH endpoint completely since the feature is now entirely obsolete.

### 3. `worker.py` Orchestrator
Fix the `now - last_prop_firm_cache_ts >= 20` loop to invoke `detector.auto_detect_challenge_type(server_name, account_name)`.

## Verification Method
Test the detection locally by writing a small Python test suite `test_auto_detect.py` that confirms common substrings evaluate out correctly.

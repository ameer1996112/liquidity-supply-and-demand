# Phase 7 Plan 3: Localize Risk engines per account

## Requirements
- EXEC-03: Guardrails (Correlation, PropGuard) enforce limits securely per-account in Redis tracking spaces.

## Steps
1. Append `:{account_id}` dynamically into Redis key strings in `PropGuard.py`.
2. Append `:{account_id}` dynamically into `CorrelationMatrix.py`.
3. Ensure daily streak tracking isolates cleanly to prevent cross-account limit explosions.

# Bugs

- 2026-06-21 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Demand zones such as the 09:30 XAU origin could show a misleading/provisional liquidity line when `Only Draw Strategy-Valid Liquidity` was disabled, because the main demand scanner still capped reference/pivot distance and could reject the visible reference liquidity above the zone | Removed the max-distance cap from the main demand scanner while keeping touch-sweep caps, matching the uncapped origin/reference fallback behavior; regression risk is limited to demand liquidity candidate selection among already side-valid candidates

- 2026-06-21 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Some supply liquidity lines were skipped after switching inducement supply to below-top matching, because zones whose reference liquidity was above the supply top had no fallback path and were rejected/cleared | Added dual-side supply candidate ranking: prefer below/inside liquidity when present, otherwise accept the closest above-top candidate; disabled inducement supply wrong-side clearing so fallback candidates persist; regression risk is limited to supply inducement candidate ranking

- 2026-06-21 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Supply inducement liquidity still diverged from the reference indicator because the scanner forced high-side `pHigh > zone.top` candidates, while the reference marks the nearest lower/inside pivot high below the supply top for zones like the 16:05 XAU supply | Restored inducement supply matching to `pHigh <= zone.top` with distance `zone.top - pHigh`, re-enabled the normal max-distance cap, made own-origin a fallback only, and changed wrong-side clearing to reject only prices above the supply top; regression risk is limited to supply inducement liquidity selection

- 2026-06-21 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | The 16:05 XAU supply zone could still use the higher external liquidity line after adding supply `OWN_ORIGIN`, because the own-origin matcher required `originHigh == zone.top`; when supply bounds were expanded above the origin candle, the correct origin-high liquidity sat inside the zone and was ignored | Changed supply own-origin matching and wrong-side exemption to accept the origin high anywhere inside `[zone.bottom, zone.top]` with tick tolerance; regression risk is limited to supply origin-bar liquidity selection on expanded supply zones

- 2026-06-21 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | The accepted 16:05 XAU supply zone could draw the farther high-side reference line instead of its own origin-high/zone-top liquidity because supply scanning had no `OWN_ORIGIN` fallback/priority, unlike demand | Added supply own-origin liquidity selection from `originHigh == zone.top`, allowed it to override farther candidates, and marked it strong immediately; regression risk is limited to inducement-mode supply zones whose origin high is the closest liquidity edge

- 2026-06-21 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Supply liquidity could attach to the wrong lower/inside line because inducement-mode supply scanning accepted highs below `zone.bottom`, while the visible/reference supply liquidity is the high-side line above the supply zone | Re-aligned supply reference, pivot, local, retracement, distance, and wrong-side checks to use `pHigh > zone.top`; regression risk is limited to supply-zone liquidity selection in inducement mode

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | The 16:05 XAU supply candidate could be marked `REJECT_WEAK_DISPLACEMENT` even with a valid drop, because historical supply confirmation scanned from the base candle down to offset `1`, excluding the current confirmation candle and including the base candle instead | Changed the historical supply departure loop to scan `candidateBaseIdx - 1` down to `0`, matching the demand path; regression risk is limited to suppressed-ACC metals historical supply confirmation timing

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | The 16:05 XAU supply zone was skipped or anchored to the prior rally candle because supply base validation required a bullish base, then the bearish-reversal fix was too broad and allowed bearish drop candles to become false supply zones | Allow XAU/XAG/XPT/XPD bearish supply bases only when they follow a bullish rally candle, and add `Debug zone detection` labels for the 16:05 supply candidate path; regression risk is limited to suppressed-ACC metals supply origins that are bearish reversal candles after a bullish approach

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | XAU stacked supply could still miss separated reference zones because historical suppressed-symbol supply recovery stopped too early on a bearish confirmation event | Replaced the one-zone boolean stop with a three-zone creation counter so the two higher supplies do not block the next separated supply; regression risk is limited to XAU/XAG/XPT/XPD bearish historical supply recovery where additional separated supply zones can now be created

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | XAU supply zones could still be missed even after stacked supply scanning because the suppressed-symbol historical recovery existed only for demand, so an older bullish supply origin confirmed by the current bearish move was never considered | Added a supply-side historical suppressed-standard recovery path and active-similar supply dedupe, mirroring the existing demand recovery; regression risk is limited to XAU/XAG/XPT/XPD bearish confirmation bars where an additional standard supply zone can now be created

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | A demand zone could stay `WAITING_PIVOT` when the relevant visible liquidity was a high-side reference on the origin candle, because demand scanning only considered demand-side low references and never tested high-side origin liquidity | Added a demand-only origin-high reference fallback that runs only after normal liquidity candidates fail, requiring the reference line to be in the origin/formation window and at/above the origin high within tick tolerance

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Origin/formation liquidity lines could be visible but skipped for a newly created zone because reference lines were marked swept before zone linking, and the scanner rejected all swept references even when their bar was the zone origin candle | Allow swept reference liquidity to seed the zone when its bar is inside the origin/formation window, preserving normal swept filtering for older unrelated lines; added `Debug liquidity detection` labels with origin and tested-line coordinates

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Demand liquidity could appear skipped when a closer candidate sat directly on the zone edge, because RawRD ranked every inducement candidate by closest distance while the strategy avoids near-edge demand liquidity when a better off-edge candidate exists | Ported the strategy's demand-only `bestLiqNearEdge` ranking into RawRD reference, pivot, and local demand liquidity selection; regression risk is limited to demand candidate selection among already-valid candidates

- 2026-06-20 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | Touch-sweep liquidity zones could remain hidden with `Only Draw Strategy-Valid Liquidity` enabled because the indicator revalidated touch-sweep candidates with strong-leg candle counting, while the strategy treats touch-sweep liquidity as one-candle valid immediately | Matched the strategy path by marking detected touch-sweep liquidity valid/swept with `liquidityCandleCount = 1`, reducing regression risk to the touch-sweep branch only

- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | Debug-only hidden liquidity probing ran on every active invalidation pass, causing TradingView `RE10110` timeouts on heavier symbols like XAUUSD 5m | Gate the expensive origin liquidity probe behind the selected-time debug label window so normal chart execution does not scan hidden liquidity for every zone
- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | Hidden/overlapped zones could show `INVALIDATION_CHECK` without exposing their linked liquidity state, making it impossible to inspect the liquidity line for a covered origin like 23:10 | Added liquidity price/bar, low/high liquidity candidates, sweep flags, source, and debug-only origin/raw-local liquidity probes to zone-state invalidation debug labels
- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | Debug Selected Zone could look empty when strict liquidity found no current candidate because labels were only emitted for creation/rejection/invalidation events, and the new status label was hidden unless Raw Mode was exactly Debug Selected Zone or the confirmed lifecycle loop ran; if the selected origin was not active, it still drew nothing | Added a last-bar selected-zone status pass and a NO_SELECTED_ZONE fallback showing zone count and selected time whenever selected-time diagnostics are enabled
- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | LAB/PROD liquidity detection diverged from the better reference strategy by accepting permissive fractal/local/equal-liquidity candidates and one-candle liquidity by default | Ported reference-style Makuchaku 3-candle pivot validation, disabled one-candle liquidity by default, and set liquidity pivot length to 10
- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | Orange demand liquidity line could still pick a shallow valid pivot because candidates were ranked by distance from the zone edge instead of the actual liquidity extreme | Rank demand liquidity by the deepest valid low and supply liquidity by the highest valid high while keeping strict/local pivot filtering unchanged
- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | One-candle/local demand liquidity was briefly mapped to the candle body-side level, which made the green demand liquidity line too shallow instead of the expected deeper `29783.01` level | Restored local liquidity line pricing to the actual low/high extreme while keeping strict/local candidate competition intact
- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | Raw local high/low fallback made liquidity detection too broad and created incorrect lines across zones | Rolled back raw local fallback; keep liquidity selection limited to strict and one-candle pivot candidates while preserving candidate visibility fixes

- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | 23:10 demand zone could choose `29793.07` instead of the lower `29782.85` liquidity because local one-candle pivots were only scanned when no strict pivot existed | Let local one-candle liquidity candidates compete with strict pivots so the closer valid level can win

- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex_LAB.pine`, `scripts/pinescript/indicators/SND_Raw_RD_Forex_PROD.pine` | Valid liquidity candidates could disappear or freeze on an older level because the scanner stopped after `liquidityValid` and line display waited for `targetSwept` | Keep rescanning until liquidity or target sweep proof and keep valid unswept liquidity candidates visible

- 2026-06-18 | `scripts/pinescript/indicators/SND_Raw_RD_Forex*.pine` | NAS100 LAB/PROD suppressed ACC bounds, so the 23:10 origin fell back to a broad standard demand zone instead of the compact upper wick-to-body zone shown by the reference | Allowed ACC bounds on index symbols and aligned ACC bounds so demand uses origin high to body high while supply uses body low to origin low

- 2026-04-23 | `src/api_positions.py` | Dashboard live positions and live account summary only used one primary broker adapter, so multi-account live positions could disappear behind signal fallback even while other accounts were connected | Added profile-based live aggregation across eligible MetaAPI and cTrader accounts and corrected the frontend fallback mapper to preserve `signal.size`

- 2026-04-23 | `scripts/pinescript/strategies/SND_Strategy.pine` | `XAGUSD` zones could still be tagged as accuracy zones because the Pine gating excluded gold, indices, and platinum but not silver | Added `is_silver` to the `should_use_accuracy_zones` guard so silver matches gold behavior at the source

- 2026-04-16 | `src/api.py` | Silent exception handling in health checks, AI mode fallback, websocket cleanup, and webhook enqueue path hid production failures at the ingress boundary | Added structured logging for those branches, deduplicated CORS origin assembly, and fail-closed with HTTP 503 when queue enqueue fails

# Project Instructions

- Only inspect files and folders I explicitly mention.
- Do not scan the whole repository by default.
- Ignore archive docs, caches, build folders, logs, and large datasets unless requested.
- Prefer plan first, then minimal patch.
- Do not rewrite unrelated files.
- For frontend work, stay inside `frontend/` unless backend changes are required.
- For backend Python work, stay inside `src/`, `scripts/`, `tests/`, and `migrations/` as needed.
- For Pine or webhook-contract work, inspect only the exact docs or scripts requested.
- Use sample data before raw full datasets whenever possible.
- Summaries must be based only on files actually inspected.